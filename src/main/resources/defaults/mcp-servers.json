{
  "servers": [
    {
      "name": "playwright-browser",
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "@playwright/mcp", "--headless"],
      "enabled": false
    }
  ]
}
